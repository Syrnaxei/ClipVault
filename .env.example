# 复制为 .env 并修改。生成随机 Key：openssl rand -hex 32
# Windows PowerShell: -join ((1..64) | ForEach-Object { '{0:x}' -f (Get-Random -Max 16) })

# 必填：API Key（所有 /api 与 /ws 请求的鉴权凭据）
API_KEY=change-me-to-a-long-random-string

# 对外访问端口（外部通过 http://<IP>:该端口 访问）
CLIPVAULT_PORT=31291
