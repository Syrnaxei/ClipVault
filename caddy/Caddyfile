:80 {
	root * /srv/www
	encode gzip

	# handle 块互斥：确保 API/WS 不被 SPA fallback 拦截（try_files 默认先于 reverse_proxy 执行）
	handle /api/* {
		reverse_proxy server:3000
	}
	handle /ws {
		reverse_proxy server:3000
	}

	handle {
		try_files {path} /index.html
		file_server
	}
}
